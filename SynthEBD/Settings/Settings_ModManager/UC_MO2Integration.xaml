<UserControl x:Class="SynthEBD.UC_MO2Integration"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             d:DataContext="{d:DesignInstance Type=local:VM_MO2Integration}"
             xmlns:local="clr-namespace:SynthEBD"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <local:UiModeToGridRowHeightConverter x:Key="UiModeRowHeight"/>
        <local:UiModeToVisibilityConverter x:Key="UiModeVis"/>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="{Binding Source={x:Static local:UiModeController.Instance}, Path=DisplayMode, Converter={StaticResource UiModeRowHeight}, ConverterParameter=Troubleshoot}"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" MinWidth="125" Text="{Binding ExecutablePathLabel}" local:DocTooltip.Key="ModManager.MO2ExecutablePath" VerticalAlignment="Center"/>
        <Button Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Command="{Binding FindExecutable}" Foreground="{DynamicResource ActionGoodForeground}" Margin="5 0 0 0">Search</Button>
        <TextBox Grid.Row="0" Grid.Column="2" Text="{Binding ExecutablePath}"/>
        <Button Grid.Row="0" Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5 0 0 0"
                Command="{Binding ToggleLinuxMode}" Content="{Binding LinuxModeButtonText}"
                Foreground="{DynamicResource ActionGoodForeground}" local:DocTooltip.Key="ModManager.MO2LinuxMode"
                Visibility="{Binding Source={x:Static local:UiModeController.Instance}, Path=DisplayMode, Converter={StaticResource UiModeVis}, ConverterParameter=Troubleshoot}"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Mod Folder Path" local:DocTooltip.Key="ModManager.MO2ModFolder" VerticalAlignment="Center"/>
        <Button Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Command="{Binding FindModFolder}" Foreground="{DynamicResource ActionGoodForeground}" Margin="5 0 0 0">Search</Button>
        <TextBox Grid.Row="1" Grid.Column="2" Text="{Binding ModFolderPath}"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="FilePath Length Limit" VerticalAlignment="Center" local:DocTooltip.Key="ModManager.FilePathLimit"/>
        <xctk:IntegerUpDown Grid.Row="2" Grid.Column="2" Value="{Binding FilePathLimit}" Foreground="{DynamicResource PrimaryForeground}" MinWidth="25"/>
    </Grid>
</UserControl>

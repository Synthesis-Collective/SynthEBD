<UserControl x:Class="SynthEBD.UC_PresetAnnotationEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SynthEBD"
             d:DataContext="{d:DesignInstance Type=local:VM_PresetAnnotationEditor}"
             mc:Ignorable="d"
             d:DesignHeight="220" d:DesignWidth="900">
    <DockPanel>
        <!-- Title bar: shows which (preset, weight) slice is being annotated. Driven by
             VM.Title which switches between the placeholder and the active-row label. -->
        <Border DockPanel.Dock="Top" Background="{DynamicResource SecondaryBackground}" Padding="6,4" Margin="0,0,0,4">
            <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
        </Border>

        <!-- Reused VM_BodyShapeDescriptorSelectionMenu. The DataContext for the inner UC is
             the menu VM itself (initialized lazily via VM.InitializeMenu); we expose it as
             VM.DescriptorMenu and bind the inner UC's DataContext to that property. -->
        <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="2"
                IsEnabled="{Binding IsEditingEnabled}">
            <ContentControl Content="{Binding DescriptorMenu}">
                <ContentControl.Resources>
                    <DataTemplate DataType="{x:Type local:VM_BodyShapeDescriptorSelectionMenu}">
                        <local:UC_BodyShapeDescriptorSelectionMenu/>
                    </DataTemplate>
                </ContentControl.Resources>
            </ContentControl>
        </Border>
    </DockPanel>
</UserControl>

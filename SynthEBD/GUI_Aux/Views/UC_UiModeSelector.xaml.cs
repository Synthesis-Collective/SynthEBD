using System.Windows.Controls;

namespace SynthEBD;

/// <summary>
/// Interaction logic for UC_UiModeSelector.xaml (the global Use/Customize/Troubleshoot selector;
/// stateless — all state lives on <see cref="UiModeController"/>).
/// </summary>
public partial class UC_UiModeSelector : UserControl
{
    public UC_UiModeSelector()
    {
        InitializeComponent();
    }
}

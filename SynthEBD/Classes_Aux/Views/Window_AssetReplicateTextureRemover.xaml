<Window x:Class="SynthEBD.Window_AssetReplicateTextureRemover"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SynthEBD"
        d:DataContext="{d:DesignInstance Type=local:VM_AssetReplicateTextureRemover}"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="Replicate Texture Remover" Height="450" Width="800"
        Style="{DynamicResource ThemedWindowStyle}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <local:BoolToSolidColorBrushConverter x:Key="BoolToBrushConverter_Multiplets"
                                              TrueColor="{StaticResource PrimaryForeground}"
                                              FalseColor="{StaticResource StatusPendingForeground}"/>
    </Window.Resources>

    <i:Interaction.Behaviors>
        <local:TitleBarThemingBehavior/>
    </i:Interaction.Behaviors>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ProgressBar Value="{Binding HashingProgressCurrent}" Maximum="{Binding HashingProgressMax}" Background="Transparent" BorderThickness="0" />
        <TextBlock Grid.Row="0" Text="{Binding CurrentlyHashingFile}" HorizontalAlignment="Center" Grid.ColumnSpan="2"/>
        <Button Grid.Row="1" Content="Find Replicate Textures" Command="{Binding CheckDuplicatesButton}" HorizontalAlignment="Left"/>

        <StackPanel Grid.Row="2"  Orientation="Horizontal">
            <TextBlock Text="Cleaning Mode: " VerticalAlignment="Center" ToolTip="{Binding ModeToolTipStr}" ToolTipService.IsEnabled="{Binding Source={x:Static local:TooltipController.Instance}, Path=DisplayToolTips}"/>
            <ComboBox SelectedItem="{Binding SelectedCleaningMode}" ItemsSource="{Binding CleaningOptions}"/>
            <Button Content="Remove Duplicate Assets" Command="{Binding CleanAssetPack}" Foreground="{DynamicResource ActionAccentForeground}" Visibility="{ Binding HasMultiplets, Converter={StaticResource BoolToVis}}"/>
        </StackPanel>

        <ScrollViewer Grid.Row="3">
            <ItemsControl ItemsSource="{Binding MultipletTextureGroups}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" Margin="0 10 0 0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="{Binding FileName}" Foreground="{DynamicResource StatusPendingForeground}" />
                                <ItemsControl Grid.Row="1" ItemsSource="{Binding FilePaths}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBox Grid.Column="0" Text="{Binding DisplayedPath}" HorizontalScrollBarVisibility="Visible" IsReadOnly="True" Foreground="{Binding IsSelected, Converter={StaticResource BoolToBrushConverter_Multiplets}}"/>
                                                <CheckBox Grid.Column="1" IsChecked="{Binding IsSelected}" Margin="5 0 0 0"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>

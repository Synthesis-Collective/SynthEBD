<Window x:Class="SynthEBD.Window_MessageWindowOK"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        d:DataContext="{d:DesignInstance Type=local:VM_MessageWindowOK}"
        xmlns:local="clr-namespace:SynthEBD"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="{Binding Header}" Height="450" Width="800"
        Style="{DynamicResource ThemedWindowStyle}">
    <i:Interaction.Behaviors>
        <local:TitleBarThemingBehavior/>
    </i:Interaction.Behaviors>
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0">
                <TextBox Text="{Binding Path=Text, Mode=OneWay}" Background="Transparent" BorderThickness="0" IsReadOnly="True" TextWrapping="Wrap" FontSize="12"/>
            </ScrollViewer>
            <StackPanel Grid.Row="1" Orientation="Horizontal">
                <Button Foreground="{DynamicResource ActionGoodForeground}" Content="OK" Command="{Binding OkCommand}" FontSize="18"/>
                <Button FontSize="18" Content="Copy Message" Command="{Binding CopyTextCommand}" Margin="15 0 0 0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
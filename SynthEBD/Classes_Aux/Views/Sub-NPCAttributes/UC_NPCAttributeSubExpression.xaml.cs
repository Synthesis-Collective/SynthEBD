using System.Windows.Controls;

namespace SynthEBD;

/// <summary>
/// Interaction logic for UC_NPCAttributeSubExpression.xaml — the inline parenthetical block hosting
/// a nested OR-collection of condition cards (rendered recursively via <see cref="UC_NPCAttribute"/>).
/// </summary>
public partial class UC_NPCAttributeSubExpression : UserControl
{
    public UC_NPCAttributeSubExpression()
    {
        InitializeComponent();
    }
}

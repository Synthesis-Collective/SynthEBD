<UserControl x:Class="SynthEBD.UC_PackagerOption"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SynthEBD"
             xmlns:plugins="clr-namespace:Mutagen.Bethesda.WPF.Plugins;assembly=Mutagen.Bethesda.WPF"
             d:DataContext="{d:DesignInstance Type=local:VM_PackagerOption}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Name: " VerticalAlignment="Center" local:DocTooltip.Key="Packager.OptionName"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Description: " VerticalAlignment="Center" local:DocTooltip.Key="Packager.OptionDescription"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Destination Folder Name: " VerticalAlignment="Center" local:DocTooltip.Key="Packager.DestinationModFolder"/>
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding DestinationModFolder, UpdateSourceTrigger=PropertyChanged}" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Directory Routing: " VerticalAlignment="Center" local:DocTooltip.Key="Packager.DirectoryRouting"/>
            <StackPanel Grid.Row="3" Grid.Column="1">
                <ItemsControl ItemsSource="{Binding FileExtensionMap, UpdateSourceTrigger=Default}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock VerticalAlignment="Center">Route ".</TextBlock>
                                <TextBox Text="{Binding [0], UpdateSourceTrigger=PropertyChanged}" MinWidth="60" HorizontalContentAlignment="Left" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}"/>
                                <TextBlock VerticalAlignment="Center">" files to "</TextBlock>
                                <TextBox Text="{Binding [1], UpdateSourceTrigger=PropertyChanged}" MinWidth="60" HorizontalContentAlignment="Left" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}"/>
                                <TextBlock VerticalAlignment="Center">" folder</TextBlock>
                                <Button VerticalAlignment="Center" Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, Path=DataContext.RemoveFileExtensionMapping}" CommandParameter="{Binding}" Foreground="{DynamicResource ActionDangerForeground}" Margin="5 0 0 0">X</Button>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <Button Height="20" VerticalAlignment="Top" HorizontalAlignment="Left" Command="{Binding AddFileExtensionMapping}" Foreground="{DynamicResource ActionGoodForeground}">Add New</Button>
            </StackPanel>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Sub-Options Prompt: " VerticalAlignment="Center" local:DocTooltip.Key="Packager.SubOptionsPrompt"/>
            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding OptionsDescription}" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}"/>

            <StackPanel Grid.Row="5" Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="Asset Config Files: " VerticalAlignment="Center" local:DocTooltip.Key="Packager.AssetConfigFiles"/>
                <Button Command="{Binding AddAssetConfigFile}" Foreground="{DynamicResource ActionGoodForeground}" HorizontalAlignment="Right">+</Button>
            </StackPanel>
            <ItemsControl Grid.Row="5" Grid.Column="1" ItemsSource="{Binding AssetPackPaths, UpdateSourceTrigger=Default}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Button VerticalAlignment="Center" Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, Path=DataContext.FindJSONFile}" CommandParameter="{Binding}" Foreground="{DynamicResource ActionGoodForeground}">Select</Button>
                            <local:UC_CollectionMemberStringDecorated DataContext="{Binding}"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <StackPanel Grid.Row="6" Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="BodyGen Config Files: " VerticalAlignment="Center" local:DocTooltip.Key="Packager.BodyGenConfigFiles"/>
                <Button Command="{Binding AddBodyGenConfigFile}" Foreground="{DynamicResource ActionGoodForeground}" HorizontalAlignment="Right">+</Button>
            </StackPanel>
            <ItemsControl Grid.Row="6" Grid.Column="1" ItemsSource="{Binding BodyGenConfigPaths, UpdateSourceTrigger=Default}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Button VerticalAlignment="Center" Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, Path=DataContext.FindJSONFile}" CommandParameter="{Binding}" Foreground="{DynamicResource ActionGoodForeground}">Select</Button>
                            <local:UC_CollectionMemberStringDecorated DataContext="{Binding}"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Grid.Row="7" Grid.Column="0" Text="Add Patchable Races"/>
            <local:UC_NamedFormKeyMultiPicker Grid.Row="7" Grid.Column="1" MaxHeight="500" FormKeys="{Binding AddPatchableRaces}" LinkCache="{Binding LinkCache}" ScopedTypes="{Binding RacePickerFormKeys, Mode=OneWay}" />

            <StackPanel Grid.Row="8" Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="Record Template Plugins: " VerticalAlignment="Center" local:DocTooltip.Key="Packager.RecordTemplatePlugins"/>
                <Button Command="{Binding AddRecordTemplateFile}" Foreground="{DynamicResource ActionGoodForeground}" HorizontalAlignment="Right">+</Button>
            </StackPanel>
            <ItemsControl Grid.Row="8" Grid.Column="1" ItemsSource="{Binding RecordTemplatePaths, UpdateSourceTrigger=Default}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Button VerticalAlignment="Center" Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, Path=DataContext.FindPluginFile}" CommandParameter="{Binding}" Foreground="{DynamicResource ActionGoodForeground}">Select</Button>
                            <local:UC_CollectionMemberStringDecorated DataContext="{Binding}"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <StackPanel Grid.Row="9" Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="Ignore Missing Source Files: " VerticalAlignment="Center" local:DocTooltip.Key="Packager.IgnoreMissingSourceFiles"/>
                <Button Command="{Binding AddIgnoredSourceFile}" Foreground="{DynamicResource ActionGoodForeground}" HorizontalAlignment="Right">+</Button>
            </StackPanel>
            <ItemsControl Grid.Row="9" Grid.Column="1" ItemsSource="{Binding IgnoreMissingSourceFiles, UpdateSourceTrigger=Default}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBox Text="{Binding Content}" MinWidth="250"/>
                            <Button Content="x" VerticalAlignment="Center" Command="{Binding DeleteCommand}" Foreground="{DynamicResource ActionDangerForeground}"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <StackPanel Grid.Row="10" Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="Mod Downloads: " VerticalAlignment="Center" local:DocTooltip.Key="Packager.ModDownloads"/>
                <Button Command="{Binding AddDownloadInfo}" Foreground="{DynamicResource ActionGoodForeground}" HorizontalAlignment="Right">+</Button>
            </StackPanel>
            <ItemsControl Grid.Row="10" Grid.Column="1" ItemsSource="{Binding DownloadInfo, UpdateSourceTrigger=Default}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:UC_DownloadInfoContainer DataContext="{Binding}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </ScrollViewer>
</UserControl>

<UserControl x:Class="SynthEBD.UC_DownloadInfoContainer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SynthEBD"
             d:DataContext="{d:DesignInstance Type=local:VM_DownloadInfoContainer}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Mod Page Name: " VerticalAlignment="Center" local:DocTooltip.Key="Packager.DownloadModPageName"/>
            <Border Grid.Row="0" Grid.Column="1" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" >
                <TextBox Text="{Binding ModPageName}"/>
            </Border>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Mod Page URL: " VerticalAlignment="Center" local:DocTooltip.Key="Packager.DownloadUrl"/>
            <Border Grid.Row="1" Grid.Column="1" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" >
                <TextBox Text="{Binding URL}"/>
            </Border>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Mod Download Link Text: " VerticalAlignment="Center" local:DocTooltip.Key="Packager.DownloadLinkText"/>
            <Border Grid.Row="2" Grid.Column="1" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" >
                <TextBox Text="{Binding ModDownloadName}"/>
            </Border>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Expected File Name: " VerticalAlignment="Center" local:DocTooltip.Key="Packager.DownloadExpectedFileName"/>
            <Border Grid.Row="3" Grid.Column="1" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" >
                <TextBox Text="{Binding ExpectedFileName}"/>
            </Border>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Override Prefix" VerticalAlignment="Center" local:DocTooltip.Key="Packager.DownloadOverridePrefix" />
            <Border Grid.Row="4" Grid.Column="1" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" >
                <TextBox Text="{Binding ExtractionSubPath}"/>
            </Border>

            <Button Grid.Row="5" Grid.Column="0" Content="Delete" VerticalAlignment="Center" Command="{Binding DeleteCommand}" Foreground="{DynamicResource ActionDangerForeground}" Margin="5 0 0 0"/>
        </Grid>
    </Border>
</UserControl>
